<Window x:Class="kt_4.ContractsWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:kt_4"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ContractsWindow">

    <Grid Margin="0,0,0,-6">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="0*"></RowDefinition>
        </Grid.RowDefinitions>
        <DataGrid x:Name="DGridContracts" AutoGenerateColumns="False"  Height="441" VerticalAlignment="Center" IsReadOnly="False" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="id" Binding="{Binding id}" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="Номер контракта" Binding="{Binding ContractNumber}" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="Дата контракта" Binding="{Binding ContractDate}" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="id сотрудника" Binding="{Binding EmployeeID}" Width="*"></DataGridTextColumn>
                <DataGridTemplateColumn Width="auto">
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
